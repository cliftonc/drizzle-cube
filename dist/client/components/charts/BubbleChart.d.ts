import { default as React } from 'react';
import { ChartProps } from '../../types.js';
declare const BubbleChart: React.MemoExoticComponent<({ data, chartConfig, displayConfig, queryObject, height, colorPalette }: ChartProps) => React.JSX.Element>;
export default BubbleChart;
