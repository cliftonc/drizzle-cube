import { default as React } from 'react';
import { ChartProps } from '../../types.js';
declare const LineChart: React.MemoExoticComponent<({ data, chartConfig, displayConfig, queryObject, height, colorPalette, onDataPointClick, drillEnabled }: ChartProps) => React.JSX.Element>;
export default LineChart;
