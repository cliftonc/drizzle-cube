import { default as React } from 'react';
import { ChartProps } from '../../types.js';
declare const DataTable: React.MemoExoticComponent<({ data, chartConfig, displayConfig, queryObject, height }: ChartProps) => React.JSX.Element>;
export default DataTable;
