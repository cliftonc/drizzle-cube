import { default as React } from 'react';
import { ChartProps } from '../../types.js';
declare const KpiText: React.MemoExoticComponent<({ data, chartConfig, displayConfig, height, colorPalette }: ChartProps) => React.JSX.Element>;
export default KpiText;
