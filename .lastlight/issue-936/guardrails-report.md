Test Framework: Present — vitest configured and test scripts in package.json. `npm run test` runs Vitest.

Linting: Present — ESLint configured (.eslintrc.json, eslint.config.mjs). `npm run lint` runs.

Type Checking: Present — tsconfig.json and `npm run typecheck` script using tsc are configured.

CI Pipeline: Present — .github/workflows includes CI with lint/typecheck/build/test jobs.

All critical guardrails are present.
