// Type stub for the side-effect import of `@xyflow/react/dist/style.css`.
// Mapped in via tsconfig `paths` because the package ships no `.d.css.ts`
// alongside its stylesheet, and TS7 requires one under allowArbitraryExtensions.
declare const stylesheet: string
export default stylesheet
