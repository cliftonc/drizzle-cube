{
  "name": "drizzle-cube-hono-example",
  "version": "1.0.0",
  "description": "Complete Hono app example with drizzle-cube integration",
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:server": "tsx watch src/index.ts",
    "dev:client": "cd client && npm run dev",
    "dev:full": "concurrently \"npm:dev:server\" \"npm:dev:client\"",
    "dev:worker": "wrangler dev",
    "build": "npm run build:client && npm run build:worker",
    "build:client": "cd client && npm run build",
    "build:worker": "tsc --project tsconfig.worker.json && cp dist/src/worker.js dist/worker.js && cp dist/src/analytics-routes.js dist/analytics-routes.js && cp dist/src/dashboard-config.js dist/dashboard-config.js",
    "build:full": "npm run build",
    "start": "node dist/index.js",
    "deploy": "npm run build && wrangler deploy",
    "deploy:staging": "npm run build && wrangler deploy --env staging",
    "cf:login": "wrangler auth login",
    "cf:whoami": "wrangler whoami",
    "install:client": "cd client && npm install",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:logs": "docker-compose logs -f",
    "docker:reset": "docker-compose down -v && docker-compose up -d",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "tsx scripts/migrate.ts",
    "db:seed": "tsx scripts/seed.ts",
    "db:studio": "drizzle-kit studio",
    "setup": "npm run docker:up && sleep 5 && npm run db:generate && npm run db:migrate && npm run db:seed",
    "setup:neon": "npm run db:generate && npm run db:migrate && npm run db:seed"
  },
  "dependencies": {
    "@hono/node-server": "^1.8.0",
    "@neondatabase/serverless": "^1.0.1",
    "drizzle-cube": "file:../..",
    "drizzle-orm": "^0.44.4",
    "hono": "^4.0.0",
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250816.0",
    "@types/node": "^20.0.0",
    "concurrently": "^8.2.0",
    "drizzle-kit": "^0.31.4",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.78.12"
  },
  "keywords": [
    "analytics",
    "drizzle",
    "hono",
    "cube",
    "semantic-layer"
  ],
  "author": "Your Name",
  "license": "MIT"
}
